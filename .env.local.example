# DataGolf API
DATAGOLF_API_KEY=
# Optional: override the base URL for DataGolf feeds
# DATAGOLF_BASE_URL=https://feeds.datagolf.com

# Vercel KV (Upstash Redis) — used to snapshot the cut after round 2.
# These are auto-populated when you provision Vercel KV from the Vercel dashboard.
KV_REST_API_URL=
KV_REST_API_TOKEN=
